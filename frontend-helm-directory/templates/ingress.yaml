apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress-controller
spec:
  rules:
  - host: {{ .Values.host}}
    http:
      paths:
      - path: /api/v1
        pathType: {{ .Values.pathType}}
        backend:
          service: 
            name: {{ .Values.ingressService.serviceName}}
            port:
              number: {{ .Values.ingressPort.number}}    
  ingressClassName: {{ .Values.ingressClassName}}
